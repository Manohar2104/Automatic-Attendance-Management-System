package com.automatic.attendance.student

import android.app.Application

class App : Application() {
    override fun onCreate() {
        super.onCreate()
        // Initialize DI, network, analytics (none in skeleton)
    }
}
